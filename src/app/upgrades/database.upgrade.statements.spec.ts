import { DatabaseUpgradeStatements } from './database.upgrade.statements';

describe('DatabaseUpgradeStatements', () => {
  it('should create an instance', () => {
    expect(new DatabaseUpgradeStatements()).toBeTruthy();
  });
});
